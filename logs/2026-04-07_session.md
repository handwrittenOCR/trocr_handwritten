# Session Log — 2026-04-07

## Tasks completed

### 1. Fixed `compare_ner_models.py` loading error
- `ActRecord` was being constructed from `ner_regex.json` (NER results) instead of `acts_dataset.json` (raw acts)
- Added `ACTS_DATASET` path constant pointing to `NER_datasets/raw/acts_dataset.json`
- Fixed `load_acts_and_regex()` to load `ActRecord` from `acts_dataset.json` and join on `act_id`
- All 9,678 acts in `acts_dataset.json` have `act_type="unknown"`; type is resolved from `ner_regex.json`
- Fixed sampling to filter on `ner_regex.json` act types, then patch `act_type` onto `ActRecord` objects

### 2. Added `import random` to `compare_ner_models.py`

### 3. Fixed markdown table formatting
- `escape_md()` now strips `\n` and `\r` from values before building table rows
- Manually fixed broken rows in `ner_model_comparison_2026-04-07.md` (6 multiline `death_date` / `declaration_date` regex values)

### 4. Compare regex with Flash and Flash-Lite

- manual review of 5 random transcription
- Flash is better particularly on dates. On several occasions, Flash-Lite writes "ce jour" and slightly hallucinates. But overall, it's pretty good.
- **Decision**: go with Gemini 3 - Flash.
- **TODO** : cost estimation with ALL TOKENS USED

### 5. Cost estimation

- `gemini.py` now uses native SDK for NER cost extraction (same as OCR) to extract all token types
- Confirmed thinking tokens are saved correctly in `api_costs.jsonl` (flash-lite: 632 tokens, flash: 0 — expected)
- Cost estimate for 9,700 acts saved to `logs/ner_cost_estimate_2026-04-07.md`
  - gemini-3-flash-preview: **€10.67**
  - gemini-3.1-flash-lite-preview: **€6.21** (~42% cheaper)
- Both costs are low; cost is not a deciding factor between models

### 6. Batch NER pipeline (`scripts/ner/run_ner_llm.py`)

- Reads `NER_datasets/raw/acts_dataset.json`, skips already-processed act IDs
- Filters by `--commune`, `--year`, `-n`; `--budget EUR` stops when adjusted cost threshold hit
- Processes in chunks of 500 (incremental save after each chunk)
- Output: `NER_datasets/llm/ner_llm.json` (single merged file, all communes)
- Ran on abymes: 1,203 acts, 100% entity extraction, 0 still-unknown after LLM (702 décès, 497 naissances, 4 mariages)

### 7. Marge fields added to LLM extraction (`trocr_handwritten/ner/llm_extractor.py`)

- Added `_MARGE_FIELDS` dict spread into all 4 tool schemas
- Added `_extract_marge_fields()` helper
- `extract()` now populates `marge_act_type`, `marge_act_name` (slave's name), `marge_act_number`, `marge_act_owner` in every `NERResult`
- Existing abymes results have nulls for these fields — need re-run to populate

### 8. Cleaning pipeline (`trocr_handwritten/ner/cleaning/`)

- `dates.py`: French date parser — declaration_date parsed first, event dates resolved using it as reference; `dernier`/`courant` = previous/current month relative to declaration date; outputs ISO strings
- `ages.py`: French number words → int
- `entities.py`: fuzzy clustering (rapidfuzz) of owner and plantation names → canonical name + separate integer ID sequences
- `export.py`: reads `ner_llm.json`, applies all cleaning, writes `NER_datasets/llm/cleaned/ner_birth.csv`, `ner_death.csv`, `ner_marriage.csv` (UTF-8 BOM for R/Excel compatibility)
- `scripts/ner/export_cleaned.py`: runner, supports `--commune` filter

### 9. Updated `scripts/ner/README.md`

- Added cleaning pipeline paths and usage instructions

### 10. Repo cleanup & git hygiene (mb_prepare_launch → main)

- Reviewed all files for production vs experimental code
- Identified `scripts/testing/`, `test_model_logging.py`, `tests/test_lowthinking/` as experimental-only
- Strategy: create `experiments` branch (retains all history), delete experimental files from `mb_prepare_launch` before merge
- Installed `uv` via PowerShell; bumped `aiohttp` to `>=3.13.4` in `pyproject.toml` + `uv.lock` to fix 10 Dependabot alerts
- Added `logs/` to `.gitignore` (outputs stay local, not on GitHub)
- API cost logs now mirrored to Dropbox: `CostTracker.DROPBOX_LOG_DIR` hardcoded in `cost_tracker.py`; `log_summary()` writes to both paths automatically
- Session logs also saved to Dropbox path going forward (updated `CLAUDE.md` + memory)

### 11. NER quality audit script (`scripts/ner/audit_quality.py`)

- Reads cleaned CSVs (`ner_death.csv`, `ner_birth.csv`, `ner_marriage.csv`) and raw `ner_llm.json`
- Three modes: `stats` (fill rates, numeric stats, value counts), `problems` (high nulls on key fields, non-ISO dates, age outliers, marge/act_type mismatches, duplicate act_ids), `sample` (N random acts with raw marge + plein texte + extracted fields table)
- Filters: `--act-type`, `--commune`, `--n`, `--seed`, `--output`
- First audit saved to `logs/ner/audit_2026-04-07.md`
- Design is generic (date/age naming conventions) — reusable for other OCR projects

### 12. Fixed marriage NER extraction (`config/ner_marriage.prompt` + `trocr_handwritten/ner/llm_extractor.py`)

- Audit showed spouse2 missing in 3/4 marriage acts; all age/registration fields null for both spouses
- Root cause: prompt gave no structural signal that two full person records needed extraction; schema fields were undescribed and not required
- Prompt: added "typical structure" block with a concrete example, added "RÈGLE CRITIQUE" section explicitly requiring all fields for both spouses
- Schema: added explicit descriptions to all 12 spouse fields; `spouse2_name` flagged as "OBLIGATOIRE — toujours présent"; added `spouse1_age`, `spouse1_registration_number`, `spouse2_age`, `spouse2_registration_number` to `required`
- Marriage acts in abymes need re-run to populate corrected fields

- `config/ner_marriage.prompt`: spouse1 = époux (homme), spouse2 = épouse (femme); qualifier-based sex detection; fallback to order of appearance if sex indeterminate
- `trocr_handwritten/ner/llm_extractor.py`: all spouse field descriptions updated to reflect male/female distinction

### 13. Date cleaning improvements (`trocr_handwritten/ner/cleaning/dates.py`)

- Bracketed illegible tokens stripped in `_normalise` (`[illisible]`, `[novembre?]`, etc.)
- Fuzzy month matching via `rapidfuzz` as fallback when exact match fails
- Year regex now handles 19th-c spelling variants: "cens", "huis", "huip" (e.g. "mil huit cens quarante")
- `_parse_french_day` now searches anywhere in the string (was anchored to start — broke on any prefix)
- "hier" → declaration_date − 1 day; "avant-hier" → −2 days; "ce jour"/"aujourd'hui" → same day as declaration
- `parse_declaration_date` accepts `year_registry` fallback; partial month+year parse returns day=1 proxy
- Removed all weekday-stripping logic (no longer needed)
- Result: 52 → 4 unresolvable missing declaration_dates on abymes (remainder are genuinely illegible)

### 14. Missing month inference (`trocr_handwritten/ner/cleaning/export.py`)

- `_fill_missing_months`: for acts where declaration_date is still null after parsing, borrows month from nearest neighbour act within same commune × year_registry group
- `_patch_csv_missing_months`: CSV round-trip wrapper; called from `export_all` for all three act types

### 15. Merged act detection and splitting

- New `trocr_handwritten/ner/split_acts.py`: detects merged plein_texte (multiple "L'An mil huit cent" headers) and splits into separate `ActRecord` instances with `_split1`, `_split2`... suffixed IDs; marge preserved on first segment only
- `scripts/ocr/build_all_datasets.py`: calls `split_merged_acts` after `build_dataset` so `acts_dataset.json` already contains expanded records before NER

### 16. `split_acts.py` fixes and improvements

- Regex `_ACT_HEADER` anchored to line start (`(?m)^\s{0,10}l.{0,2}an\s+mil\s+huit`) — prevents false splits on mid-text year references (e.g. birth dates containing "mil huit cent")
- Removed `aujourd` from `_ACT_HEADER` — "Aujourd'hui" splits are already handled by `split_registries()` in `dataset.py`
- Added length gate: only records above 1.5× the median length for the batch are candidates for splitting
- Debug print shows first 1000 chars of merged act line-by-line (120 chars/line) when a split occurs
- Gosier/1840-style "Aujourd'hui" splitting is handled separately in `dataset.py:split_registries()`, not here

### 17. `raw_plein_texte` added to NER output CSVs

- `export.py`: `raw_plein_texte` now 4th column in `ner_birth.csv`, `ner_death.csv`, `ner_marriage.csv`


### Pending

- Re-run `build_all_datasets.py` to regenerate `acts_dataset.json` with split records
- Re-run NER on abymes to populate marge fields + fix marriage extraction (4 acts)
- Run cleaning tests on key variables for Abymes (birth, death, mariages) `notebook_cleaning_llm.py`
    - allows for indentifying whether there is need for some changes in the llm prompting + start building the cleaning pipeline
- Full NER run on all remaining communes (~8,500 acts)
- **Objective**: Full run and cleaning by Friday 10th.
