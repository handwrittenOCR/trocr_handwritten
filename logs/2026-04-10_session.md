# Session Log — 2026-04-10

## Tasks completed

### 1. `run_ner_llm.py` — safe output file handling

- Added `resolve_output_path()`: if `ner_llm.json` already exists, saves to `ner_llm_1.json`, `ner_llm_2.json`, etc. (never overwrites)
- Added `merge_all_runs()`: merges all `ner_llm*.json` files into `ner_llm_all.json` (overwritable); called automatically at the end of each run
- `save_merged_results` now accepts `output_path` parameter instead of hardcoded `NER_OUTPUT`
- Print statement informs user when output is redirected to a new filename

### 2. Owner/plantation cluster linkage

- Updated `export_cleaned.py` to auto-detect `{commune}_clusters.json` from `PAIRS_DIR`; added `--clusters` override arg
- Added `append: bool = False` to `_write_csv`, `export_deaths`, `export_births`, `export_marriages`, `export_all` in `export.py`
- All-communes mode now processes per-commune, applying each commune's clusters file when present and falling back to fuzzy otherwise
- Fixed missing `from pathlib import Path` import in `entities.py`

### 3. Date cleaning — fix cascading month corruption

**Root cause**: `_fix_declaration_month_order` enforced ascending months by neighbor majority vote. A single hallucinated date (e.g. March on page 006 where OCR produced "commune d'aubigny Canton de Caudebec") cascaded forward, corrupting all subsequent February acts.

**Fix** (`export.py`): Replaced neighbor-cascade logic with event-date anchor. Now only corrects declaration month when it's inconsistent with the event month (birth/death/marriage). Rule: `declaration_month - event_month` must be 0 or 1; otherwise declaration month is set to event month.

### 4. OCR quality flag

**Problem**: LLM sometimes confidently hallucinates text for hard-to-read crops (0% `[illisible]` but wrong commune/year). Needs a quality signal.

**Added** `_ocr_quality()` in `export.py` — scores each act as `good`, `low`, or `bad`:
- `bad`: `[illisible]` ratio > 50%, or year mismatch (including "mil sept cent" = 1700s), or empty text
- `low`: `[illisible]` ratio > 20%, or commune name not found in text
- `good`: passes all checks

Added `ocr_quality` column to all three CSV exports (births, deaths, marriages).

**abymes distribution**: 838 good, 60 low, 305 bad (out of 1203 acts).

### 5. Export dedup + sort

- Added `_dedup_records()` in `export_cleaned.py`: removes duplicate `act_id`s (keeps last occurrence), sorts by `act_id` (commune → year → page → reading order)
- Applied at load time before any commune filtering or export

### 6. `run_ner_llm.py` — fix failed-act retry

- `load_existing_results()` now reads from `ner_llm_all.json` (if exists) instead of only `ner_llm.json`, correctly counting all processed acts across multiple runs
- Failed act_ids (from `failed_ner.json`) are removed from the "done" set so they appear as pending on retry

### 3. Dedup in export pipeline

- Added `_dedup_records()` in `export_cleaned.py`: removes duplicate `act_id` entries (from overlapping LLM runs), keeps first occurrence

## Pending (carried from 2026-04-09)

- Write `abymes_clusters.json` with all confirmed owner + plantation clusters
- Update `generate_clustering_prompt.py` to include unique plantation names
- Update `cluster_owners_claude.py` to output clusters with `type` field
- ~~Update `export_cleaned.py` to pass `clusters_json_path` to `export_all()`~~ ✓
- ~~Re-run `export_cleaned.py --commune abymes` with Claude clusters applied~~ ✓
- Run `extract_owner_pairs.py` for all communes
- Run NER on abymes to populate marge fields + fix marriage extraction (4 acts)
- Full NER run on all remaining communes (~8,500 acts)
