# Session Log — 2026-04-09

## Tasks completed

### 1. Created `scripts/ner/notebook_audit_variables.ipynb`

- Audit notebook for LLM-extracted variables on abymes cleaned CSVs (births + deaths)
- Covers: `_dates` (declaration + event), `_age`, `owner_id`, `plantation_id`
- For each variable: fill-rate, value distribution, missing examples with raw text, random examples, discrepancy analysis, proposed cleaning steps
- `owner_id` section includes: raw→clean mapping, singleton clusters, normalised-name inspection, near-duplicate pairs (score 70–84), threshold comparison 85 vs 80 with full diff of new merges

### 2. Date cleaning fixes (`trocr_handwritten/ner/cleaning/dates.py`)

**Root cause of all day-parsing bugs**: `_parse_french_day` was run on the full string including the year phrase (`mil huit cent quarante quatre`), so number words from the year — `huit` (8), `quatre` (4), `six` (6) — were matched as the day before the correct word.

**Fixes**:
- Added `_find_month_position(norm)` — finds the month token in the text with its position
- Added `_parse_day_from_context(norm, month_start)` — looks at 1–4 tokens immediately before the month token, stripping `du mois de/d'` filler; never touches year words (which come after the month)
- `parse_declaration_date` and `parse_event_date` now parse month first, then anchor day extraction to what's before it
- Fixed leading apostrophe handling: `^['\u2019]?\s*l.?an\s+` (handles `'An mil...` style)
- Raised fuzzy month match cutoff from 75 → 80 to prevent `"matin"` matching `"mai"` (fuzz.ratio = exactly 75%)
- Added `"dix huis": 18` to `_FRENCH_DAYS` (OCR variant of `dix-huit`)

**Verified cases**:
- `"Samedi Seize novembre Mil huit cent quarante quatre"` → `1844-11-16` ✓ (was `1844-11-04`)
- `"dix du mois d'octobre"` → day=10 ✓ (was day=1)
- `"samedi seize mars mil huit cent quarante quatre"` → `1844-03-16` ✓ (was `1844-03-04`)
- `"hier soir à onze heures"` → declaration_date − 1 day ✓ (was using 11 as day)
- `"dix huis du mois de Novembre"` → declaration day=18 ✓ (was 10)

### 3. Export/cleaning pipeline fixes (`trocr_handwritten/ner/cleaning/export.py`)

**New pipeline in `_patch_dates` (called after each CSV write)**:

1. `_fill_declaration_dates` — borrows month from nearest neighbour in same commune × year group, sets day=1
2. `_fill_event_dates` — sets event_date = declaration_date where event_date is missing or year-only
3. `_normalise_years` — overrides year to folder_year always; event month=12 in first 10% of acts → folder_year−1
4. `_fix_declaration_month_order` — ascending month enforcement within commune × year, majority-vote neighbors
5. `_fix_event_date_anomalies` — sets event_date = declaration_date when event_date > declaration_date OR > 60 days before

**Age validation** (`trocr_handwritten/ner/cleaning/ages.py`):
- Added `parse_age_validated(text, min_age)`
- declarant_age < 21 → None; mother/father/spouse age < 15 → None

**Tests**: `tests/test_cleaning.py` — 5 tests for `_fix_declaration_month_order`, all passing.

### 4. Owner/plantation entity resolution pipeline

**Problem**: fuzzy auto-clustering in `entities.py` (threshold 85) merges too aggressively and misses OCR variants that look different but refer to the same person (e.g. `Julien Les armagnac` → `Louis Léo Armagnac`). Manual review of abymes (owner, plantation) pairs showed many such cases.

**New pipeline**:
1. `scripts/ner/extract_owner_pairs.py` — reads all 3 cleaned CSVs, writes `{commune}_owner_pairs.json` (unique owner × plantation pairs with counts) to `NER_datasets/llm/owner_pairs/`
2. `scripts/ner/generate_clustering_prompt.py` — generates a copy-pasteable prompt pointing to the pairs file; Claude reads it, produces `{commune}_clusters.json`
3. `scripts/ner/cluster_owners_claude.py` — same via API (requires `ANTHROPIC_API_KEY`)
4. `trocr_handwritten/ner/cleaning/entities.py` — added `load_clusters_lookup(path, entity_type)` that reads Claude clusters JSON and returns `{raw_name: (canonical, entity_id)}`
5. `trocr_handwritten/ner/cleaning/export.py` — `export_all()` now accepts `clusters_json_path`; uses Claude clusters when present, falls back to fuzzy

**Clusters JSON format** (one file per commune, covers both owners and plantations):
```json
[
  {"type": "owner", "canonical": "Gabriel Vernias", "variants": ["Vernias", "Sieur Vernias", ...]},
  {"type": "plantation", "canonical": "Longval", "variants": ["Long-val", "Longual", "Longeval"]}
]
```

**Key insight**: owner and plantation are independent dimensions — one owner may have multiple plantations and vice versa. Clustering is done separately for each type but stored in one file.

**abymes analysis completed**: full (owner, plantation) pair list reviewed, clusters identified (see conversation). Pending: write the final `abymes_clusters.json`.

## Pending

- Write `abymes_clusters.json` with all confirmed owner + plantation clusters from the conversation analysis
- Update `generate_clustering_prompt.py` to include unique plantation names in the same prompt
- Update `cluster_owners_claude.py` to output clusters with `type` field
- Update `export_cleaned.py` to pass `clusters_json_path` to `export_all()`
- Re-run `export_cleaned.py --commune abymes` with Claude clusters applied
- Run `extract_owner_pairs.py` for all communes
- Run NER on abymes to populate marge fields + fix marriage extraction (4 acts)
- Full NER run on all remaining communes (~8,500 acts)
